"""쓴 글이 없는 사용자를 위한 기본 문체.

템플릿 소스는 분석할 원문이 없으므로 분석가를 돌리지 않는다. 대신 여기 있는
StyleGuide 를 그대로 작가에게 넘긴다. 토큰 0으로 분석 단계를 건너뛰는 것이
이 경로의 핵심이고, 소스가 무엇이든 StyleGuide 로 수렴한다는 설계 원칙은
그대로 유지된다.

어투는 일부러 비워두듯 중립으로 적는다. 말투(경어체/구어체)는 사용자가
글 요청에서 직접 고르고, 작가 프롬프트에서 그 선택이 문체 가이드의 어투보다
우선하기 때문이다.
"""

from __future__ import annotations

from server.models import StyleGuide

DEFAULT_STYLE_GUIDE = StyleGuide(
    tone="담백하고 간결한 기술 설명체. 과장이나 감탄 없이 사실과 근거로 서술한다.",
    sentence_ending=(
        "문장을 짧게 끊는다. 같은 종결 어미를 세 번 이상 연달아 쓰지 않는다."
    ),
    avg_paragraph_sentences=3,
    heading_style="명사형 소제목을 기본으로 하고, 문제 상황을 다루는 절에서만 의문형을 쓴다.",
    code_example_ratio=(
        "실행 방법을 다루는 절에는 실제로 돌아가는 코드나 명령을 넣고, "
        "실행 결과 또는 확인 방법을 바로 뒤에 붙인다."
    ),
    vocabulary=(
        "외래어는 통용 표기를 쓰고(도커, 쿠버네티스), 처음 등장하는 전문 용어는 "
        "그 자리에서 한 줄로 풀이한다."
    ),
    structure_pattern=(
        "핵심을 앞에 두고(두괄식) 본문에서 근거와 예시를 채운 뒤, "
        "마지막 절에서 요약과 다음 단계를 정리한다."
    ),
    dos=[
        "주장마다 근거(수치, 코드, 공식 문서의 동작)를 붙인다.",
        "예시는 실제 상황처럼 구체적으로 쓴다. '어떤 API'가 아니라 '주문 목록 API'처럼.",
        "긴 나열은 표나 리스트로 정리한다.",
    ],
    donts=[
        "'~하면 좋습니다' 같은 일반론으로 문단을 채우지 않는다.",
        "확실하지 않은 수치·버전을 단정하지 않는다.",
        "본문과 무관한 인사말이나 잡담으로 글을 열지 않는다.",
    ],
    referenced_posts=[],
)
